# TermuxPro 产品迭代 Backlog

本文件记录当前可验证事实，不把首版发布视为产品完成。每轮迭代关闭最高优先级纵向切片后继续审计。
产品边界：Termux 原本已经具备的终端基础能力只做兼容守护，不作为主要研究和重造方向；TermuxPro 的
主要价值应落在远程工作区、AI CLI、tmux/Git 可视化管理、自定义快捷指令、移动端交互和工具箱等增值
服务上。任何增值改动必须确保原始终端能力不受影响，失败时回退到原始终端仍可用。
用户反馈明确要求：原本 Termux 已有的功能不再进入研发研究队列，TermuxPro 只打磨增值服务，同时把
原始能力纳入兼容回归，防止增值层污染基础终端体验。
选题门禁：新增研发切片必须先证明属于 TermuxPro 增值服务；若只是 Termux 原有基础能力，只能进入
兼容回归清单，不能作为主线需求继续研究或重做。评审时必须写明分类：远程工作区、AI CLI、tmux/Git
可视化、自定义快捷指令、移动端工作流效率、上下文工具箱，或原始能力兼容守护；只有前六类进入主线
迭代，最后一类只用于证明 TermuxPro 没有破坏官方 Termux 原有能力。
产品负责人在拆需求时必须先回答“这是不是 TermuxPro 增值层”。如果只是原始终端、PTY、包管理、本地
shell、基础会话、基础快捷键或基础文件能力，不进入产品主线，只补不破坏回归、上游同步风险和安全回退。
真正进入主线的工作应围绕手机 SSH 远程开发、Claude/Codex CLI、tmux/Git 管理、自定义快捷指令和低频
工具箱，把用户从记命令、找入口、判断风险和恢复上下文的负担中解放出来。

## P0

| 状态 | 任务 | 证据与验收重点 |
|---|---|---|
| 进行中 | 消除所有深色页面黑字 | 已替换系统列表项、建立设计令牌并拆分不透明产品主题；Git 工作台分支/远端跟踪弹窗已改为显式深色列表项和统一按钮配色，并补 Robolectric 回归；本轮补强 AlertDialog 深色主题、运行时背景/标题/正文/按钮/列表项样式，并新增 `test/dialog-readable-style-test.sh` 阻止产品代码直接展示未套样式的系统弹窗；当前切片进一步把自定义快捷指令、tmux 会话、AI CLI 启动、提示词编辑器和 Git 工作台展示统一到 `TermuxProDialogStyle.show/prepare`，静态门禁禁止增值页手写 `setOnShowListener`，避免黑字问题重复出现；10 个关键页面纳入默认/200% 字体截图，待 Android API/字体矩阵复验 |
| 进行中 | SSH 首次连接闭环 | 诊断页按网络/端口、服务器指纹、身份认证、远端环境四阶段展示可证明进度；仅指纹待确认或认证失败时提供安全交互入口，指纹变化时禁止快捷继续；本轮新增手机粘贴输入容错：工作区 SSH 地址支持 `user@host`、`user@host:port`、`ssh -p port user@host` 和 `ssh://user@host:port/path`，保存、连接、Git/tmux/文件/诊断/SSH Key/Web 预览统一标准化为单一 OpenSSH 目标参数和端口；待 Actions、153 远端服务器联调与 Android 运行时复验 |
| 进行中 | SSH/tmux 连接策略与会话隔离 | 已实现四种工作区策略和全量 tmux 会话显式管理；Actions 增加回环 sshd/tmux 真实 fixture，验证应用 OpenSSH 进程链、输出限制、缺失 tmux 和归属隔离；待 CI、153 远端服务器联调与 Android 运行时复验 |
| 进行中 | Claude Code 共享账号隔离 | 首页与终端快捷入口均默认新建；历史会话必须进入 Claude/Codex 原生选择器后由用户显式选择，待远端 fixture 与真机复验 |
| 待办 | 重建真机验收证据 | 每个 Pass 绑定机型、系统、APK SHA、步骤、截图或脱敏日志 |
| 已完成 | 发布版本单一来源 | `termuxpro-version.properties` 已驱动 Gradle、构建脚本、产物目录和 Release 工作流，并经候选标签与 0.2.0 正式构建验证 |
| 已完成 | 0.4.0-rc.2 发布前验收门禁 | PR #48 已将 Release 改为先验收待发布签名 APK 再创建公开 Release，并消除研发 PR action_required 噪声；`v0.4.0-rc.2` Release run `33379101232` 已验证签名 APK 覆盖升级先于公开 Release 创建 |
| 已完成 | 0.9.7 稳定版发布 | `0.9.7-rc.1` 已完成候选 Release、签名 APK 构建、覆盖升级验收和附件核验；`0.9.7` 正式版已通过 `dev → master` 发布 PR、master CI、稳定标签、正式 Release workflow、附件核验和 dev 对齐 CI。稳定证据见 `test/reports/0.9.7-release-readiness.md` |
| 已完成 | 0.9.8-rc.1 候选版发布 | `0.9.7` 后 `dev` 已积累 AI CLI 会话中心项目任务入口和项目任务执行目标上下文两个用户可见增值改动；`0.9.8-rc.1` 已完成候选 PR、GitHub CI、dev 收尾 CI、正式签名 APK 构建、Release 模拟器覆盖升级验收和附件核验；发布评审见 `test/reports/0.9.8-rc.1-release-readiness.md` |
| 进行中 | 0.9.9-rc.1 候选版发布 | `0.9.8-rc.1` 后 `dev` 已继续合入终端触摸滚动模式入口、Git 最近提交摘要、快捷指令场景分组和 Git 下一步建议等用户可见体验切片；由于候选冻结范围扩大，不复用 `0.9.8-rc.1`，本轮递增到 `0.9.9-rc.1` 并按候选发布门禁验证 |
| 进行中 | 远程 AI 会话中心 | 增值服务分类：AI CLI、远程工作区、tmux 可视化、Git 可视化、移动端工作流效率和上下文工具箱。tmux 全量查看、显式进入和 TermuxPro 自有会话停止已实现；Codex/Claude 已支持新建与历史选择。本轮新增工具箱内 AI CLI 会话中心，只展示当前工作区、Claude/Codex 新建/历史命令预览、共享账号/tmux 风险和相关工具入口，不读取 AI 私有历史、不自动进入 tmux、不改变 Termux 原始终端行为；当前切片将中心页从说明页推进为可执行行动中心，增加 Claude/Codex 新建与历史 4 个显式入口，统一打开独立 SSH 终端并固定 `ssh_only` 策略，工作区无效时退回工作台配置；同时补齐 AI 完成后的下一步入口，可从会话中心直接查看 Git 工作台概览/改动；本轮新增“检查 AI 远端环境”入口，启动 Claude/Codex 前可直接进入只读诊断，核对项目路径、tmux、Git、Node/Java、Claude 和 Codex 是否可用，不读取 AI 历史、不自动进入 tmux；同时在 AI 中心展示工作区 tmux 策略和 AI 快捷启动策略差异，明确 AI 启动始终只建立 SSH，不继承自动 tmux，降低共享账号误入风险；本轮将 AI 中心信息架构调整为“当前上下文 / 启动前先确认 / Claude / Codex / AI 完成后”行动分组，把环境预检、tmux 选择、Git 检查、快捷指令和工作区配置放到对应阶段，减少按钮堆叠和解释成本；当前切片继续补齐 AI 完成后的“运行项目任务”入口，让开发者从 AI 结果检查直接进入项目任务/测试页，避免把验证命令误输入到正在工作的 Claude/Codex TUI 中；待补历史元数据展示和安全管理能力 |
| 进行中 | 项目任务执行目标上下文 | 增值服务分类：远程工作区、移动端工作流效率和上下文工具箱。项目任务页会在远端目录执行测试/构建/开发脚本，多工作区和共享服务器场景下必须显式显示 `host:port · path`；当前切片在项目任务页增加可复制的执行目标，错误态也保留目标展示，避免用户只看到失败提示却不知道失败发生在哪个远端目标；不改变项目识别、命令确认、独立终端启动和 Termux 原始终端能力 |
| 已完成 | AI CLI 启动上下文防误入 | 增值服务分类：AI CLI、远程工作区、移动端工作流效率。PR #176 已合入 dev：工作台入口继续展示远程目标并创建独立远程终端；终端工具箱入口明确提示命令会发送到当前可见终端，并在新建/历史操作项中预览 `claude`、`claude --resume`、`codex`、`codex resume` 等实际命令；本地 pre-push、分支 CI、Emulator UI 和 dev 收尾 CI 均通过，证据见 `test/reports/ai-launch-context-clarity-20260901.md` |
| 进行中 | 增值服务优先与原始能力守护 | 不再把 Termux 原有终端基础能力作为重复研发方向；每轮选题必须先通过增值服务准入检查，优先远程/AI/tmux/Git/快捷指令/移动 UX/工具箱增值层；滚动、快捷键、本地会话、PTY、原始 shell 可用性只作为“不受影响”守护测试和回归面 |
| 进行中 | Git 分支切换安全门禁 | 增值服务分类：Git 可视化、远程工作区、移动端工作流效率。Git 工作台已有分支列表、创建、删除和远端跟踪入口，本轮补强“脏工作区禁止切换”双门禁：UI 直接提示先 stash/提交/处理，命令层返回 77 并不执行 `git switch` 或 `git switch --track`，避免手机端误切分支污染远程开发上下文；不研究或重做 Termux 原始终端能力 |
| 进行中 | Git 最近提交摘要 | 增值服务分类：Git 可视化、远程工作区、移动端工作流效率。Git 工作台虽然已有“提交记录”二级入口，但手机端概览页应直接帮助开发者判断当前分支最近发生了什么；当前切片在概览区新增最近 3 条提交摘要，复用已有 `git log -20` 只读协议输出，不新增远端写操作，证据见 `test/reports/git-recent-commits-summary-20260908.md`；后续继续补提交详情、按作者/时间筛选和与 AI 修改检查联动 |
| 进行中 | Claude 快捷指令模板防误续接 | 增值服务分类：AI CLI、自定义快捷指令、远程工作区。快捷指令模板不得使用 `claude --continue` 静默续接共享账号最近会话；模板统一复用 AI 启动策略，明确区分 Claude 新建独立会话与历史选择，历史入口只进入 CLI 原生选择器，不自动污染其他项目上下文 |
| 已完成 | 快捷指令模板降低手机输入成本 | 增值服务分类：自定义快捷指令、AI CLI、tmux/Git 可视化和移动端工作流效率。PR #174 已合入 dev：新增 Codex、Claude、Git、tmux 和前端检查模板，模板只预填编辑器，不自动保存或执行，并继续走危险命令和秘密检测；本地 pre-push、分支 CI、手动 Emulator UI 和 dev 收尾 CI 均通过，证据见 `test/reports/custom-command-templates-20260901.md` |
| 进行中 | CI 成本与失败噪声治理 | 文档/规则/验收证据类变更不应触发模拟器和全量 Android 构建；CI 保留静态门禁，自动 PR 按路径判断是否等待 UI run；合并后 dev 收尾 CI 必须按 merge commit 与 dev 分支查询 workflow_dispatch，避免实际成功后 workflow 空等超时并发送失败邮件。2026-09-01 复核最近 100 条 Actions，真实 failure 仅来自已修复的 0.9.0-rc.2 发布链路，近期 cancelled 属于并发取消或被替代 run，不等同代码失败；本轮发现自动 PR job 使用 `gh pr merge` 可能把自身 pending check 纳入等待形成自等待，已改为在完整 CI/模拟器 UI 通过后调用 Pulls merge API；后续每轮合入后先核查失败 run 再进入下一切片 |
| 进行中 | 建立持续产品体验闭环 | 已定义体验原则、核心旅程、独立角色驳回权、度量和门禁；首轮产品/UI 审计已完成且结论为不通过，待 QA 独立审计和按纵向旅程推进整改 |
| 进行中 | 修复 200% 字体关键页面溢出 | PR #25 已修复首页和 AI 选择；PR #28 已修复 Git/远程目录/文件预览；PR #29 当前证据确认 tmux、项目任务和连接诊断的返回/刷新/恢复入口在 200% 字体下完整可见。代码正文保留格式化横向滚动，待补可选自动换行、行号/跳转与英文矩阵 |
| 已完成 | 连接验证状态绑定真实目标 | PR #34 已改为 24 小时可信时效、过期状态与就地重检，同时保留经过 SSH 主机身份/认证保护的 AI 快捷启动；默认/200% 字体模拟器和全量 CI 通过。后续仍需绑定规范化目标/主机指纹摘要，并在异步结果写入前再次核对目标 |
| 已完成 | 修复全局反馈提示空白 | PR #35 将终端前台反馈改为显式配色的应用内横幅；空文案、生命周期、线程路由、共享 Logger、无障碍和布局测试通过，默认/200% 字体模拟器证据可读。厂商真机复验保留到 0.3.0 候选门禁 |
| 已完成 | 终端导航入口可发现性 | PR #36 将易误认的会话图标和普通标题改为常驻“会话 / 工作台”文字入口，右侧快捷操作可横向滚动；两轮 UI 驳回后，360dp 默认/200% 字体截图、全模块测试和模拟器回归通过 |
| 已完成 | 首页信息架构瘦身与工具箱入口显性化 | PR #55 已合入 dev：首页主操作改为打开远程终端，工作区管理入口继续收敛为“服务器与项目”，终端纯图标工具入口改为文字“工具箱”；低频 Git/文件/任务/Web 预览继续不得回流首页。本地全量测试、GitHub CI、模拟器 UI 和 dev 收尾 CI 均通过 |
| 已完成 | 终端触摸滑动必须滚动历史输出 | 用户反馈 `0.5.0` 体验包中终端上下滑动被转换为命令行上下键，无法查看上方历史输出；`0.5.1` 起已改为触摸拖动、惯性滑动和 Shift+Page 强制滚动 scrollback，鼠标滚轮保留程序内滚动；`0.8.0` 继续补强默认滚动入口，所有非鼠标来源默认强制 scrollback，避免厂商输入栈或兼容路径再次退化为 DPAD_UP/DPAD_DOWN；`0.8.1` 增加“触摸滚动模式”，默认保护 shell 输入，同时允许在 Claude/Codex/vim/less 等支持鼠标追踪的 AI/TUI 中滚动内部面板，并把切换入口放进终端工具箱当前上下文；本轮继续补强 TermuxPro 增值层可发现性，终端顶栏直接显示“工具箱 · 滑动看历史/滑动控 TUI”，菜单项明确“当前手势语义 + 切换目标”，Toast 明确说明手指上下滑动后的实际行为，避免用户把旧包或错误模式误判为终端原始滚动失效；`test/terminal-touch-scrollback-test.sh`、`TerminalTouchScrollPolicyTest` 与 `TerminalProjectToolsMenuTest` 均锁定该规则，证据见 `test/reports/terminal-scroll-mode-clarity-20260908.md`，后续若新版复现输入污染则重新升为 P0 |
| 已完成 | 0.8.0 稳定版发布 | `v0.8.0` 已发布为正式版，覆盖工作区上下文、最近状态、终端触摸滚动补强和 Git 本地分支安全删除；发布证据见 `test/reports/0.8.0-release-readiness.md` |
| 已完成 | 0.8.1 稳定版发布 | `v0.8.1` 已发布为正式版，覆盖终端触摸滚动模式、终端工具箱快捷切换、本地会话关闭/行级菜单、AI CLI 启动安全提示、自动 PR 失败噪声治理和推送前冒烟门禁；Release run `33479114252`、master CI `33479102374` 和 dev 对齐收尾 CI `33479765528` 均通过，APK SHA-256 为 `cace2b90382ea84fb46a9f47d8d59ad272625792f24f35eb7b95cc5097898abe` |
| 已完成 | 0.9.0-rc.1 候选版发布 | `v0.9.0-rc.1` 已发布为 Pre-release，冻结 0.8.1 之后的终端工具箱增值入口：Git、tmux、项目任务、远端文件、Web 预览、环境诊断、SSH 密钥，以及 CI 快速通道和自动 PR 收尾修复；Release run `33497699230` 通过签名 APK 构建和覆盖升级验收，APK SHA-256 为 `de7369a06b15ce35090028aa2683a8598b2ebce7e5e9a41f342abe0f536e2de7`；发布评审见 `test/reports/0.9.0-rc.1-release-readiness.md` |
| HOLD | 0.9.0-rc.2 候选版发布 | `0.9.0-rc.1` 后 dev 合入发布等待兜底和 AI CLI 上下文提示；由于后者是用户可见体验改动，`0.9.0` 正式版不得直接跳过候选验证。本轮冻结 `0.9.0-rc.2`，但 Release run `33501845633` 在发布设备验收脚本空输出路径失败，公开 Release 未创建；修复见 `test/reports/release-device-gate-empty-output-20260901.md`，下一步递增 `0.9.0-rc.3` |
| 已完成 | 0.9.0-rc.3 候选版发布 | `v0.9.0-rc.3` 已发布为 Pre-release，保留 rc.2 增值体验冻结范围，补齐 Release 设备验收脚本空输出诊断和回归；Release run `33504695802` 通过签名 APK 构建、覆盖升级验收和公开 Pre-release 创建；APK SHA-256 为 `acefb97ad6000d0a5f8dc860bace8ff03984e13dba7ce668c2c73302bce3331d`；不覆盖、不删除、不移动 `v0.9.0-rc.2` 失败证据；发布评审见 `test/reports/0.9.0-rc.3-release-readiness.md` |
| 已完成 | 0.9.0 稳定版发布 | `v0.9.0` 已发布为正式版，保留 rc.3 冻结范围并补齐 Release 设备验收脚本空输出诊断；稳定发布 PR #147、master CI run `33507109744` 和 Release run `33507561359` 均通过；APK SHA-256 为 `9818c7b081537d04c74b9390cf878030718dd841619ebf931341296a924ed355`；发布评审见 `test/reports/0.9.0-release-readiness.md` |
| 已完成 | 0.9.1-rc.1 候选版发布 | `v0.9.0` 后 `dev` 已积累 tmux 归属细分、AI CLI 启动目标上下文、Git 工作台远端目标和 Actions 失败噪声复核等用户可见/维护可见增值改动；`v0.9.1-rc.1` 已发布为 Pre-release，候选 PR #154、分支 CI `33515746823`、自动 PR `33515746920`、dev 收尾 CI `33516217907` 和 Release run `33516755674` 均通过；APK SHA-256 为 `ee34b660a8b448d796864413ccdd6bc4188ac8179914b018e9d2d1b125fba5d3`；发布评审见 `test/reports/0.9.1-rc.1-release-readiness.md` |
| 已完成 | 0.9.1 稳定版发布 | `v0.9.1` 已发布为正式版，沿用 `0.9.1-rc.1` 已验证冻结范围，不加入新功能；发布 PR #157、CI OOM 修复 PR #158、master CI run `33521956638` 和 Release run `33522531577` 均通过；APK SHA-256 为 `ff0de6b1471ba01dcb6273aba6881862b0e67bad016d6852aeab5278b198d4c4`；发布评审见 `test/reports/0.9.1-release-readiness.md` |
| 已完成 | 0.9.4-rc.1 候选版发布 | `v0.9.3` 后 `dev` 已积累快捷指令模板、AI CLI 启动上下文防误入、自动 PR 创建稳定性和增值服务边界规则；`v0.9.4-rc.1` 已发布为 Pre-release，候选 PR #178、分支 CI `33551347585`、自动 PR `33551347733`、dev 收尾 CI `33551828340` 和 Release run `33552386533` 均通过；APK SHA-256 为 `a978b62d0285bfd463a7af8e5c108d324fb7bfa146b725b3c06e964c3070e788`；发布评审见 `test/reports/0.9.4-rc.1-release-readiness.md` |
| 已完成 | 0.9.4 稳定版发布 | `v0.9.4` 已发布为正式版，沿用 `0.9.4-rc.1` 已验证冻结范围，不新增功能、不研究或重做 Termux 原始能力；稳定准备 PR #180、发布 PR #181、master CI run `33555492800`、Release run `33556037501` 和 dev 对齐 CI `33556861451` 均通过；APK SHA-256 为 `6c86aac8cae7205ab819f41dbaa1dc20889fffce6d2acdf547ad54457122c5e4`；发布评审见 `test/reports/0.9.4-release-readiness.md` |
| 已完成 | 0.9.5-rc.1 候选版发布 | `v0.9.4` 后 `dev` 已积累 AI CLI 会话中心、Git Stash 工作台、Git 分支脏工作区门禁和 Claude 快捷指令模板防误续接等用户可见增值改动；`v0.9.5-rc.1` 已发布为 Pre-release，候选 PR #187、分支 CI `33565316927`、自动 PR `33565317117`、dev 收尾 CI `33565789899` 和 Release run `33566239673` 均通过；APK SHA-256 为 `27cb49d5a211df77cb3c484f19e5cc90dfd4589bc11f4e44cb9edb94ea21302b`；发布评审见 `test/reports/0.9.5-rc.1-release-readiness.md` |
| 已完成 | 0.9.5 稳定版发布 | `v0.9.5` 已发布为正式版，沿用 `0.9.5-rc.1` 已验证冻结范围，不加入新功能、不研究或重做 Termux 原始能力；稳定准备 PR #189、发布 PR #190、master CI run `33568886879`、Release run `33569301291` 和 dev 对齐 CI `33570015461` 均通过；APK SHA-256 为 `d398e7c197fc2a681e1b19b8a3f4a0e61f747f0073dcb77ed0e980a76a4d6b97`；发布评审见 `test/reports/0.9.5-release-readiness.md` |
| 已完成 | 0.9.6-rc.1 候选版发布 | `v0.9.5` 后 `dev` 已积累深色弹窗防线、SSH 粘贴输入容错、AI CLI 会话中心行动入口、AI 后 Git 检查入口和自动 PR 噪声治理等用户可见/维护可见增值改动；`v0.9.6-rc.1` 已发布为 Pre-release，候选 PR #198、分支 CI `33577247576`、自动 PR `33577247534`、dev 收尾 CI `33577630829` 和 Release run `33578001332` 均通过；APK SHA-256 为 `3ff940a108b6a6f4d8ae853fa4a4feebcecfa8012841686be9e829647210dc45`；发布评审见 `test/reports/0.9.6-rc.1-release-readiness.md` |
| 已完成 | 0.9.6 稳定版发布 | `v0.9.6` 已发布为正式版，沿用 `0.9.6-rc.1` 已验证冻结范围，不加入新功能、不研究或重做 Termux 原始能力；稳定准备 PR #200、发布 PR #201、master CI run `33579991374`、Release run `33580009841` 和 dev 对齐 CI `33580577509` 均通过；APK SHA-256 为 `c35aef428375fe2535ae5db97a3f8d2cee19ddf4321a931cae5d60575a2e6793`；发布评审见 `test/reports/0.9.6-release-readiness.md` |
| 已完成 | 0.9.7-rc.1 候选版发布 | `v0.9.6` 后 `dev` 已积累 AI CLI 远端环境预检、AI/tmux 策略边界可见化和 AI 会话中心行动分组等用户可见增值改动；`v0.9.7-rc.1` 已发布为 Pre-release，候选 PR #206、分支 CI `34180385034`、自动 PR `34180385006`、dev 收尾 CI `34180732842` 和 Release run `34181059768` 均通过；APK SHA-256 为 `08e3be044388bc261a363104ac1af167093b66d025daa0ccdf8fd3f2ff103522`；发布评审见 `test/reports/0.9.7-rc.1-release-readiness.md` |
| 已完成 | 消除 tmux 名称前缀伪归属 | PR #30 已改为工作区 UUID、目标/端口/路径摘要、tmux server PID、session ID 与创建时间联合核对；名称前缀不再授予管理权限，同名替换必须失败关闭。真实 SSH/tmux fixture、全量 CI 与模拟器 UI 已通过；清除应用数据后的旧会话安全降级为归属未知，不自动认领 |
| 已完成 | 修复发布设备门禁运行时证据空文件 | `0.6.0-rc.3` Release run `33418901155` 覆盖升级成功，但 artifact 中 `test-artifacts/release-apk/android-runtime.txt` 为空；PR #81 已修复验收脚本，后续即使没有 AndroidRuntime 崩溃也会写入设备型号、Android 版本、ABI、输入法、进程 PID 和无崩溃结论，并补 Fatal Exception 失败回归 |

### 已完成纵向切片：Git 工作台基础闭环（0.3.0）

- 用户与场景：开发者进入远程项目后，需要先确认当前仓库、分支和工作树状态，再查看分支、切换分支、
  审查修改和最近提交，不应为了常用只读信息反复输入 Git 命令。
- 当前证据：0.2.0 只有“Git 修改审查”，成功态把分支、状态和 Diff 混成一段终端文本；没有当前分支、
  修改计数、分支列表、分支切换或提交记录入口，错误态也无法区分非 Git 仓库与 SSH/路径故障。
- 优先级：P1（高频开发工作流缺少状态感知和导航闭环）。
- 期望结果：首屏展示仓库路径、当前分支或 detached HEAD、未提交修改数量与上游同步状态；提供“分支、
  修改、提交”三个清晰入口。分支列表区分本地/远端，切换必须显示目标并在脏工作树时二次确认；所有操作
  完成后刷新真实状态，不伪造成功。
- 非目标：本轮不实现提交、推送、强制切换、重置、变基或删除分支；这些高风险写操作须另立切片。
- 已启动角色：产品、UI/UX、Git 领域架构、Android 开发、安全和 QA。
- 产品驳回条件：用户仍需从原始 Git 输出猜当前分支/修改数量，或无法从同一页面到达分支与提交记录。
- 设计驳回条件：状态层级不清、分支切换缺少当前/目标对照、空仓库与错误态混淆，或 360dp/200% 字体裁切。
- 安全驳回条件：分支名未经 Shell 转义、允许任意文本执行、脏工作树静默切换，或把失败展示成成功。
- QA 驳回条件：普通分支、detached HEAD、无上游、干净/脏工作树、非 Git 仓库与切换冲突缺少测试证据。
- 完成证据：PR #37 已合入 `dev`。产品页可直接确认仓库路径、当前分支、修改数量和上游同步状态，
  可查看本地/远端分支、显式切换本地分支、审查修改与最近提交；真实临时 Git 仓库测试、全量 CI、
  模拟器默认字体和 200% 字体截图均通过。高风险写操作仍按原约束进入后续独立切片。

### 已完成纵向切片：自定义快捷指令（0.3.0）

- 用户与场景：开发者在不同服务器和项目中反复运行构建、测试、日志、Git 或 AI 辅助命令；内置工具无法
  覆盖个人工作流时，应能把可靠命令保存为工作区工具，而不是每次在软键盘上重新输入。
- 产品目标：在终端内按需工具箱中提供用户指令管理，支持创建、查看、编辑、复制、排序、启停和删除；
  每条指令可配置名称、命令、工作目录、分组和执行前确认级别，并按工作区隔离保存。
- 首个交付闭环：完成“空状态 → 新建 → 校验 → 保存 → 列表 → 预览实际命令 → 确认执行 → 编辑/删除”
  全路径；取消编辑、非法输入、工作区切换、应用重启和命令执行失败均不得丢失或串用数据。
- 安全边界：不支持在模板中保存密码、Token 或私钥；命令必须展示最终展开结果和目标工作区，高风险命令
  默认二次确认；用户输入不能拼接进未转义的内部控制命令，日志与诊断包必须脱敏。
- 产品驳回条件：只能新增不能维护，离开首页才能使用，预置与用户指令无法区分，或用户看不到命令将在哪个
  服务器、目录执行。
- UI/UX 驳回条件：入口仍堆在首页、空状态没有明确动作、编辑页在 360dp/软键盘/200% 字体下不可完成，
  删除与执行使用相同视觉层级，或列表只能靠难辨认图标操作。
- QA 驳回条件：CRUD、排序/启停、持久化、工作区隔离、变量校验、危险确认、执行失败恢复和升级迁移任一
  缺少自动化或运行时证据。
- 非目标：首轮不做云同步、跨设备共享、公共指令市场，也不允许指令绕过 SSH 主机身份与工作区可信状态。
- 当前证据：已完成工作区隔离存储及新增、查看、编辑、复制、排序、启停、删除；终端工具箱可进入独立
  页面，列表直接展示命令、目标服务器和目录，执行使用全新 SSH 会话。每次确认与仅危险命令确认具有
  不同行为，常见秘密拒绝落盘，失效工作区选择失败关闭而不回退其他服务器。聚焦 JVM/Robolectric、
  中文生产主题、205dp/200% 字体布局和 Lint 已通过；GitHub Actions 全模块测试与模拟器默认/200% 字体
  截图通过。产品验收确认入口未回流首页、目标明确且 CRUD 闭环；UI 验收曾因深色弹窗按钮不可读和
  超大字体标题断行为三行两次驳回，修复后的字段标签、操作层级、单行标题与滚动编辑能力通过复核。

### 当前纵向切片：远程会话中心生命周期（0.4.0）

- 用户与场景：开发者通过 SSH 进入远程项目后，需要在十秒内找到自己的 tmux 工作上下文，并在手机上
  安全完成新建、查看、重命名、进入、停止与失败恢复；不能因为共享 Unix/Claude 账号误改他人会话。
- 产品目标：TermuxPro 自有会话提供完整生命周期，归属未知会话只允许显式进入；没有 tmux 时保留普通
  SSH 退路。AI 默认新建，历史恢复继续进入 Codex/Claude 原生选择器，不解析或暴露共享历史内容。
- 安全边界：每次重命名、进入和停止前重新核验 owner、工作区指纹、tmux server PID、session ID 与创建
  时间；同名冲突失败关闭，名称使用严格便携字符集，任何路径禁止默认 `tmux kill-server`。
- 产品驳回条件：用户仍需返回首页或手输命令才能新建/重命名/停止，未知归属可写，或创建后展示伪成功。
- UI/UX 驳回条件：空态没有下一步，进入与停止同视觉层级，深色弹窗文字不可读，或 360dp/200% 字体下
  新建、取消、错误恢复不可达。
- QA 驳回条件：缺少名称边界、同名冲突、归属变化、外部删除、真实回环 SSH/tmux 生命周期及无
  `kill-server` 证据；只通过静态布局不得宣称运行时完成。
- 当前进度：已实现会话中心内显式新建、重命名、进入和停止，复用既有精确进入/停止和归属链。本轮继续
  增强共享 tmux 环境的判断信息：枚举会话不再按冒号切分名称，行内展示创建/活跃时间，并将归属拆分为
  当前工作区、其他工作区、其他 TermuxPro 使用者、归属标记不完整和未标记会话；非当前工作区会话只
  允许显式进入，禁用重命名和停止，避免误改其他项目或共享账号中的他人上下文；当前继续补强列表安全
  排序和页面解释，当前工作区会话优先展示，同类内按最近活跃排序，顶部明确提示“其他归属只读管理”，
  减少共享账号下误点未知/他人会话或误解按钮缺失的概率；待完成真实回环 sshd/tmux、全量 CI、
  360dp 默认/200% 字体截图与运行时复验。

## P1

### 0.2.0 真机反馈：开发者工作流与可发现性

- 产品发现机制升级：产品负责人每轮不只消费用户已提出的功能点，必须以至少一条真实开发者旅程主动
  记录目标、频率、阻力、替代动作、误操作风险和退出路径；交付问题清单、取舍理由和验收结论。不得把
  “页面有入口”视为需求完成，也不得用控件数量代替开发效率。
- Git 工作台纵向切片：展示当前仓库/当前分支、可切换分支、本地与远端分支、未提交文件数量与状态、
  提交历史和刷新状态；提供安全的分支切换/新建、差异查看、暂存/取消暂存、提交等高频操作。冲突、脏
  工作树、detached HEAD、远端失败和危险丢弃必须明确解释并可恢复；不提供违背 Git 不可变历史语义的
  “删除提交”伪 CRUD。
  当前推进：远端分支不再停留在只读说明；用户可从明确选择的 `origin/xxx` 创建同名本地跟踪分支并切换，
  `origin/HEAD` 被过滤，已有本地分支时失败关闭，脏工作树仍由 Git 拒绝覆盖并保留修改。当前补充“新建
  本地分支并切换”入口，分支名由本地保守校验和远端 `git check-ref-format --branch` 双重校验，不推送、
  不覆盖已有分支。继续补齐提交前 index 管理：概览拆分展示已暂存/未暂存文件数，支持确认后暂存全部
  或取消暂存全部，并补充“提交已暂存修改”入口；提交前必须展示已暂存/未暂存范围差异，提交说明只允许
  单行可见文本，操作只创建本地提交，不推送、不自动暂存、不丢弃工作区文件。当前推进按文件暂存管理：
  概览新增“按文件”入口，基于 `git status --porcelain=v1 -z` 展示已暂存/未暂存/混合状态，并允许对
  单个文件执行暂存或取消暂存；操作不提交、不推送、不丢弃、不重置。当前继续补齐上游状态感知：
  概览展示具体跟踪分支，支持只刷新上游远端的 fetch；fetch 不 merge、不 rebase、不修改工作树或 index。
  已补充安全快进拉取：仅在工作树干净、存在上游且落后上游时启用 `git pull --ff-only`，拒绝自动
  merge/rebase，失败时保留工作树并提示处理路径。已补充安全推送：仅对已配置上游的当前分支执行普通
  非 force push，分支落后上游或游离 HEAD 时阻止，推送成功后刷新本地远端跟踪状态。本轮继续补充
  本地分支删除：只列出非当前且通过安全命名校验的本地分支，使用 `git branch -d`，不 force、不删除远端、
  未合并时由 Git 拒绝并保留。本轮补充安全 stash 管理：概览展示 stash 列表，允许将当前工作树保存为
  含未跟踪文件的 stash；只在工作树干净时应用指定 stash，且不使用 `pop`，应用后 stash 仍保留；删除
  stash 必须显式选择单条记录并危险确认，不执行 `stash clear`，不触碰远端。当前继续补强终端工具箱入口：Git 状态与审查 Diff 不再向当前 shell 注入
  原始命令，而是复用当前工作区直接打开 Git 工作台概览或 Diff 页面；工作区无效时失败关闭并提示回到
  工作台配置。当前补充多服务器上下文：Git 工作台首屏直接展示 `host:port · path`，避免用户在多个
  SSH 工作区间切换时只看到分支和路径却无法确认目标机器。本轮继续补强移动端决策体验：Git 工作台首屏
  根据游离 HEAD、未提交、已暂存、无上游、领先/落后和干净同步状态生成“下一步建议”，把最安全的下一
  步直接展示给用户，减少在横向按钮长条中猜操作的成本；不新增高风险 Git 写操作。后续继续拆分按文件
  暂存、stash 与冲突恢复。
- 项目任务入口增值化：终端工具箱“项目任务”不得再向当前 shell 注入启发式检测/测试命令，应复用活动
  工作区打开结构化项目任务页，由页面完成元数据识别、任务确认、独立持久会话启动和失败恢复；原始
  shell 仍允许用户手动运行任意项目命令。
- 自定义快捷指令：允许按工作区创建、查看、编辑、排序、复制、启停和删除指令，支持变量/确认级别/
  工作目录/图标或分组；执行前展示实际命令与目标，危险命令二次确认，秘密不进入模板、日志或同步。
  预置指令与用户指令分层，用户始终可以退出、撤销编辑或恢复默认。当前推进：快捷指令页新增场景说明，
  明确它是 Git、tmux、AI CLI、测试和发布命令的自定义补充层；列表按分组插入场景标题，避免用户把
  它理解成一组无上下文的孤立按钮。
- 终端入口可发现性：重新设计终端侧栏触发器和“移动开发工作台”入口，使用含义明确的图标、文字标签、
  首次引导与稳定位置；在 360dp、200% 字体、单手操作和软键盘打开时验证。验收包括首次使用者无需猜测
  “头像”即可在限定步骤内找到会话列表与工具箱。
- 设置视觉语义：设置页已改用 TermuxPro 产品主题，不再继承上游红色主色；颜色必须遵循信息/成功/警告/
  危险语义，危险色只用于破坏性动作或明确故障。后续继续校验深浅主题对比度和系统状态栏一致性。
- “管理工作区”概念与入口重构：明确它管理的是服务器连接、项目目录和连接后策略；入口已从“连接配置”
  进一步改为“服务器与项目”，终端返回工作台描述同步为“服务器、项目目录和远程入口”，减少抽象管理词。
  后续继续评估是否合并进工作区切换器；无法在当前页面解释价值的入口应删除或渐进展示。
- 历史会话完整生命周期：侧栏支持新建、查看、重命名（领域允许时）、切换、停止/删除和失败恢复，删除
  前明确对象、归属与影响。会话切换反馈必须显示具体结果，不能只有空白 Toast；共享 Claude 账号不得
  暴露或误删其他用户会话。当前推进：终端会话标题变化、切换与结束反馈已阻断空文案和英文硬编码退出
  提示，至少展示会话序号，避免手机端出现无文字白块或 `null - exited`。
- 本地终端会话 CRUD：终端侧栏不应只能“新建/切换/重命名”。当前推进：侧栏底部增加“关闭”入口，关闭前
  明确对象并提示只停止当前本地终端会话及其前台进程，不触碰远端 tmux 会话；会话行长按不再直接重命名，
  而是提供切换、重命名、关闭菜单，支持对指定本地会话操作；布局 smoke test 与静态交互测试锁定该入口。
- 多远程连接配置：允许新增和保存多个服务器/项目工作区，支持查看、编辑、复制、删除、排序/最近使用与
  快速切换；切换前保护未保存修改，身份、主机指纹、目录、tmux 策略和快捷指令按工作区隔离。0.2.0
  单配置迁移不得丢失现有数据。当前推进：单工作区时隐藏选择器，避免制造“只有一个选项”的假交互；
  已配置工作区首页直接展示“新建工作区”，不再只藏在服务器与项目弹出菜单里。本轮继续补齐多工作区
  切换上下文：选择器不再只显示抽象名称，而是展示工作区名称、服务器和项目目录，降低误连到错误服务器
  或错误项目的风险。本轮继续加入最近状态提示，区分“尚未验证、最近打开、最近验证、验证过期、需处理、
  检查失败和状态未知”，让用户在连接前判断是否进入正确上下文。
- 控件与选项规模匹配：只有一个有效选项时不使用下拉选择器，直接展示固定值或隐藏控件；有多个真实
  场景时再使用选择器并解释差异。优先审计“远程开发”工作区类型，避免用选择器制造不存在的选择成本。
- tmux 会话管理纵向切片：支持安全的新建、查看、重命名、显式进入、停止/删除和刷新；区分 TermuxPro
  自有、归属未知和其他用户会话，不自动进入、不越权管理。所有操作绑定稳定 session id 与 owner 元数据，
  单会话退出只能 `kill-session`，任何产品路径不得触发默认 `kill-server`。
- 对象生命周期设计规则：对工作区、快捷指令、应用自有会话等可变对象，设计时必须逐项检查创建、读取、
  修改、删除/归档、取消、错误恢复和跨页面一致性；对 Git 提交、远端共享会话等受领域约束对象，使用
  安全的查看、筛选、恢复或受限管理替代机械 CRUD，并在界面解释限制。

- 体验/品牌：桌面应用名已固定为 `TermuxPro` 并有 Manifest 回归测试；后续发布继续纳入桌面名称与图标检查。
- 按完整旅程重排 Roadmap：可信连接 → 十秒恢复 → 手机 AI 编码循环 → 判断 AI 修改 → 运行与预览；研发治理事项与用户阻断项分栏维护。
- 分阶段拆分巨型 `WorkspaceActivity`：先抽取模型/Repository/校验与迁移测试，再建独立编辑页、状态 reducer、可信状态和一次性终端启动薄桥；不触碰 Termux Service/PTY/bootstrap 不变量。
- 建立无障碍与输入法门禁：常驻 label/`labelFor`、TalkBack 状态播报、48dp 命中区、`adjustResize`、IME Next/Done、首错聚焦、中文 composing 和草稿/旋转/进程恢复。
- 重构首页信息架构：PR #25 已完成未配置、已配置、已验证的渐进展示和单一主操作；继续补“最近 tmux/AI 会话”和可直接继续的任务上下文。
- 将 Git、远程文件、项目任务、后台任务、SSH 密钥和 Web 预览迁入终端开发场景的按需工具箱；首页不平铺低频控件，用户无需返回首页才能调用工具。当前进展：终端工具入口已从纯图标改为文字“工具箱”，菜单按“当前上下文 / 项目与 Git / 连接与安全 / AI 工作 / 键区切换”分组，降低首次发现和长列表扫读成本；Git 状态/Git Diff/tmux 会话已改为打开移动工作台或会话中心，不再把用户带回原始终端输出阅读模式；项目任务、远端文件、Web 预览、环境诊断和 SSH 密钥也复用活动工作区打开结构化页面或独立隧道会话，不向当前 shell 注入命令，不影响原始终端手动操作。AI 工作分组已补充“启动 Claude Code”和“启动 Codex CLI”，复用既有安全弹窗与显式新建/历史选择，避免用户在终端现场找不到 AI 工作入口或把确认/拒绝误认为启动入口；终端页 AI 启动弹窗也会展示当前活动工作区的服务器、端口和项目路径，无有效工作区时明确失败关闭。
- 精简工作区管理：新增、保存、复制、删除不再作为四个同等级常驻大按钮；危险删除降级到对象菜单并明确目标。
- 合并首页重复的连接诊断/环境入口，统一加载、空、错误、成功状态与可操作修复建议。
- 建立简体中文、360dp、深色主题、默认与 200% 字体的截图矩阵；英文布局通过不能替代中文长文案验收。
- 统一未连接/组件缺失错误页：Git、远程目录、文件预览、tmux、项目任务和连接诊断已不暴露内部退出码并提供恢复动作；远端缺少 tmux 可直接打开普通 SSH。继续覆盖 SSH 组件安装动作，诊断细节按需展开。
- Claude/Codex 提供新建和选择历史会话，不跨项目恢复；Claude 共享账号场景默认新建，禁止静默续接全局最近会话。
  当前推进：AI 启动弹窗不再只给抽象的“新建/历史”两个词，选项内直接说明共享 Claude 账号推荐新建，
  历史入口只打开 CLI 原生选择器，TermuxPro 不自动进入最近会话。本轮继续补强弹窗正文：Claude Code
  单独提示共享账号风险，Codex CLI 单独提示先确认当前项目，避免用户把两个 AI CLI 当成相同恢复模型；
  工作区入口会在启动前展示当前服务器、SSH 端口和项目路径，配置不完整时明确阻止用户误以为会进入
  正确远端上下文。
- 工作区保存独立连接后策略和指定 tmux 会话名；会话列表展示创建者不可可靠识别时明确标记“归属未知”，不提供自动进入。
- SSH 工作区已支持新增、查看、编辑、复制和删除，并在切换/新建前保护未保存修改；首页按工作区展示最近打开、已验证、待处理或失败状态，且不把打开终端误报为认证成功。
- 改造终端顶栏，满足 320–360dp、200% 字体和 48dp 触控目标。
- 200% 字体工具栏已为远程文件、文件预览、项目任务、连接诊断、Git 修改、SSH 密钥和 tmux 会话保留导航/操作空间，并增加 205dp 窄屏布局测试；工作区三按钮文字裁切另行持续审计。
- 可控 SSH fixture 已覆盖真实密钥认证、tmux 有/无、输出截断和拒绝连接；继续补 DNS、超时、主机密钥变化及 Android UI 状态自动化。
- GitHub Runner 模拟器每日/按 UI PR 运行 360dp 深色默认字体与 200% 字体截图回归，本地共享服务器不启动模拟器。
- 修正模拟器定时巡检：当前工作流只在 `dev`，GitHub cron 依赖默认分支上的 workflow，现阶段不得宣称每日自动执行；选择默认分支可用的受控触发方案，并用连续 scheduled 运行证明。
- 依赖更新进入研发线：Dependabot 的 GitHub Actions 更新必须以 `dev` 为目标并使用 `ci` 提交前缀，避免
  机器人 PR 直接打到 `master` 干扰稳定发布线；旧的 `master` 目标机器人 PR 不作为发布输入。

## P2

- 工作区健康检查、会话中心和本地脱敏诊断包。
- TalkBack、截图回归、深浅主题和厂商系统兼容矩阵。
- 构建资源遥测、缓存治理与 GitHub Runner 重任务策略。
