# TermuxPro 0.9.9-rc.1 候选发布评审记录

候选状态：`PREPARING_RC`。`0.9.8-rc.1` 之后，`dev` 已继续合入多个用户可见体验切片。由于候选冻结范围
已经扩大，本轮不复用 `0.9.8-rc.1`，而是发布新的 `0.9.9-rc.1` Pre-release。

## 版本信息

- 候选版本：`0.9.9-rc.1`
- 候选 `versionCode`：`90901`
- 上个稳定版本：`0.9.7`
- 上个稳定 `versionCode`：`90702`
- 上个候选版本：`0.9.8-rc.1`
- 发布说明：`docs/RELEASE_NOTES_0.9.9-rc.1.md`

## 冻结范围

- PR #216：终端触摸滚动模式入口与提示语义清晰化。
- PR #217：Git 工作台首屏展示最近提交摘要。
- PR #218：自定义快捷指令按场景分组展示。
- PR #219：Git 工作台首屏展示下一步建议。

## 发布门禁

1. `termuxpro-version.properties` 必须为 `0.9.9-rc.1` / `90901`，且发布说明文件存在。
2. `versionCode=90901` 必须高于 `0.9.7` 正式版的 `90702` 和 `0.9.8-rc.1` 的 `90801`。
3. 本地 `git diff --check`、Skill 校验、版本元数据校验、workflow 策略校验、Release APK 设备冒烟脚本和
   Release ABI 门禁必须通过。
4. 候选 PR 合入 `dev` 前，分支 CI 和 Emulator UI 必须通过。
5. 候选 PR 合入 `dev` 后，merge commit 收尾 CI 必须通过。
6. 标签 `v0.9.9-rc.1` 必须由自动 PR 流水线在合并提交上创建，不覆盖既有标签。
7. Release workflow 必须创建公开 Pre-release，并完成正式签名 APK 构建、覆盖升级验收和附件校验。

## 本地验收

- `./scripts/termuxpro-version.sh name`：`0.9.9-rc.1`。
- `./scripts/termuxpro-version.sh code`：`90901`。
- `./scripts/termuxpro-version.sh notes`：`docs/RELEASE_NOTES_0.9.9-rc.1.md`。
- `./scripts/termuxpro-version.sh tag`：`v0.9.9-rc.1`。
- `./scripts/validate-skills.sh`：通过。
- `./test/version-metadata-test.sh`：通过，版本为 `0.9.9-rc.1 (90901)`。
- `./test/workflow-trigger-policy-test.sh`：通过。
- `./test/release-apk-device-smoke-test.sh`：通过。
- `./test/release-abi-gate-test.sh`：通过。
- `git diff --check`：通过。
- `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/pre-push-smoke.sh origin/dev`：通过；本候选准备分支只修改版本、
  发布说明和验收文档，静态门禁完成后不重复本地 Android 运行时编译。

## 远端验收

待执行。

## 回滚策略

- 若候选分支 CI、Emulator UI、dev 收尾 CI 或 Release workflow 任一步失败，不发布稳定版。
- 若 Release workflow 在公开 Release 前失败，保留失败 tag/run 作为证据，修复后递增到新的候选版本。
- 若公开 Pre-release 已创建但发现 P0/P1 缺陷，保留该版本并发布后续候选，不覆盖同一 tag。

## 结论

待远端候选发布完成后更新。
