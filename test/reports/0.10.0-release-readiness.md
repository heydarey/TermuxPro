# TermuxPro 0.10.0 稳定发布评审记录

日期：2026-09-08

稳定版状态：`RELEASED`。`0.10.0-rc.1` 已完成候选 Release、签名 APK 构建、覆盖升级验收、附件核验和
低频用户通知；当前没有发现冻结范围内的 P0/P1 阻断证据。本轮按发布规则推进 `dev → master`、
`v0.10.0` 稳定标签、正式 Release workflow、附件核验和 `dev` 对齐收尾 CI，均已完成。

## 来源候选版

- 来源候选标签：`v0.10.0-rc.1`
- 来源候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.10.0-rc.1`
- 来源候选 APK SHA-256：
  `54bb6ad1d68f62bb37f7393724a9ec3e79f6e7993a6496433393f66f180a6ccd`
- 来源候选 Release workflow：run `34309887334`，结论 `success`

## 版本元数据

- 稳定版本：`0.10.0`
- 稳定 `versionCode`：`100002`
- 发布说明：`docs/RELEASE_NOTES_0.10.0.md`

## 冻结范围

- PR #253/#254：tmux 会话中心 200% 字体层级和证据补强。
- PR #255/#256：AI 会话中心 200% 字体层级和证据补强。
- PR #257：工作区首页明确 tmux 连接方式与共享账号提示。
- PR #260：Git 提交前确认当前分支与远程目标。
- PR #261：自动 PR Actions 可见步骤和 Summary 说明，降低等待状态误判。
- PR #262：AI 启动前风险摘要前置，明确只建立 SSH、不自动进入 tmux 或恢复历史。

## 增值服务准入

- 分类：AI CLI、远程工作区、tmux/Git 可视化、移动端工作流效率和上下文工具箱。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。原始 Termux 能力只做兼容守护和“不受影响”回归保护。
- 风险控制：稳定版不加入候选版之外的新功能；AI 启动固定 `ssh_only`；tmux 必须由用户显式进入或管理；
  历史会话只进入 Claude/Codex CLI 原生选择器。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.10.0` / `100002`，且发布说明文件存在。
2. `versionCode=100002` 必须高于 `0.10.0-rc.1` 的 `100001`。
3. `git diff --check`、`./scripts/validate-skills.sh`、`./test/version-metadata-test.sh`、
   `./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh` 和
   `./test/release-abi-gate-test.sh` 必须通过。
4. 稳定准备分支合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
5. `dev → master` 发布 PR 的 CI 和 Emulator UI 必须通过。
6. `v0.10.0` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
7. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 当前没有应用启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。
- 本轮不把 Termux 原始能力作为产品主线；只确认 TermuxPro 增值层没有修改其基础语义。
- `0.10.0-rc.1` 已完成候选发布门禁，未发现阻止同周期推进稳定版的 P0/P1。

## 稳定发布证据

- 稳定准备分支：`dev_stable0100_20260908`。
- 稳定准备提交：`c08c4ec6d95f0d0cb918e025393313e15bf53f63`。
- 稳定准备 PR：#265，已合入 `dev`，merge commit
  `2e4bfc73e61f0d14f049cc824df31057a6f205fd`。
- 稳定准备本地门禁：通过 `termuxpro-version.sh` 版本/标签/Release Notes 检查、`validate-skills.sh`、
  `test/version-metadata-test.sh`、`test/workflow-trigger-policy-test.sh`、`test/release-apk-device-smoke-test.sh`、
  `test/release-abi-gate-test.sh`、`git diff --check` 和
  `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/pre-push-smoke.sh origin/dev`。
- 稳定准备分支 CI：run `34310954699`，结论 `success`。
- 稳定准备自动 PR：run `34310954711`，结论 `success`。
- 稳定准备合入 `dev` 后收尾 CI：run `34311362105`，结论 `success`。
- 发布 PR：#266，`dev → master`，merge commit
  `43c7a5794fc89eaf72b97777d4eb94a2772e021a`。
- 发布 PR CI：run `34311832886`，结论 `success`。
- 发布 PR Emulator UI：run `34311832837`，结论 `success`。
- master 合入后 CI：run `34312260787`，结论 `success`。
- 稳定标签：`v0.10.0`，指向 `master` 发布提交
  `43c7a5794fc89eaf72b97777d4eb94a2772e021a`。
- 稳定 Release workflow：run `34312651105`，结论 `success`。
- 稳定 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.10.0`。
- Release 状态：非 Draft、非 Pre-release。
- 发布资产：`termuxpro-0.10.0-arm64-v8a.apk`、`SHA256SUMS` 和
  `APK_SIGNATURE.txt`。
- 正式 APK 大小：`33972425` bytes。
- 正式 APK SHA-256：`53b0294461ccb922663c025dac07f081826d8355d91a3f259a981050ec1582ce`。
- 签名证书 SHA-256：`10c820d392d5bd9672317067a5d11fc1250217824166961df7b385128d4b49cd`。
- APK 签名校验：v1 `true`，v2 `true`，签名者数量 `1`。
- 稳定 Release 完成后已将 `dev` 快进到 `master` 发布提交，并触发 dev 对齐 CI run `34313204226`，
  结论 `success`。
- 用户通知：按低频通知规则，仅在稳定包可安装后发送一次飞书通知，去重键 `termuxpro-release-v0.10.0`。
