# TermuxPro 0.9.7 稳定发布评审记录

日期：2026-09-08

稳定版状态：`READY`。`0.9.7-rc.1` 已完成候选 Release、签名 APK 构建、覆盖升级验收、附件核验和
低频用户通知；当前没有发现冻结范围内的 P0/P1 阻断证据。本轮按发布规则推进 `dev → master`、
`v0.9.7` 稳定标签、正式 Release workflow、附件核验和 `dev` 对齐收尾 CI。

## 来源候选版

- 来源候选标签：`v0.9.7-rc.1`
- 来源候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.7-rc.1`
- 来源候选 APK SHA-256：
  `08e3be044388bc261a363104ac1af167093b66d025daa0ccdf8fd3f2ff103522`
- 来源候选 Release workflow：run `34181059768`，结论 `success`

## 版本元数据

- 稳定版本：`0.9.7`
- 稳定 `versionCode`：`90702`
- 发布说明：`docs/RELEASE_NOTES_0.9.7.md`

## 冻结范围

- PR #203：AI CLI 会话中心新增启动前远端环境预检入口。
- PR #204：AI CLI 会话中心展示工作区 tmux 策略和 AI 快捷启动策略边界。
- PR #205：AI CLI 会话中心按行动阶段分组，减少底部按钮堆叠和上下文判断成本。
- PR #206、#207：`0.9.7-rc.1` 候选发布和候选发布证据补录。

## 增值服务准入

- 分类：AI CLI、远程工作区、tmux/Git 可视化、移动端工作流效率和上下文工具箱。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。原始 Termux 能力只做兼容守护和“不受影响”回归保护。
- 风险控制：稳定版不加入候选版之外的新功能；AI 环境预检只读；AI 启动固定 `ssh_only`；tmux 必须由用户
  显式进入或管理；历史会话只进入 Claude/Codex CLI 原生选择器。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.7` / `90702`，且发布说明文件存在。
2. `versionCode=90702` 必须高于 `0.9.7-rc.1` 的 `90701`。
3. `git diff --check`、`./scripts/validate-skills.sh`、`./test/version-metadata-test.sh`、
   `./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh` 和
   `./test/release-abi-gate-test.sh` 必须通过。
4. 稳定准备分支合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
5. `dev → master` 发布 PR 的 CI 和 Emulator UI 必须通过。
6. `v0.9.7` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
7. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 当前没有应用启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。
- 本轮不把 Termux 原始能力作为产品主线；只确认 TermuxPro 增值层没有修改其基础语义。
- `0.9.7-rc.1` 已完成候选发布门禁，未发现阻止同周期推进稳定版的 P0/P1。

## 稳定发布证据

- 稳定准备分支：`dev_stable097_20260908`。
- 稳定准备提交：待回填。
- 稳定准备 PR：待回填。
- 稳定准备本地门禁：待回填。
- 稳定准备分支 CI：待回填。
- 稳定准备自动 PR：待回填。
- 稳定准备合入 `dev` 后收尾 CI：待回填。
- 发布 PR：待回填。
- 发布 PR CI：待回填。
- 发布 PR Emulator UI：待回填。
- master 合入后 CI：待回填。
- 稳定标签：待回填。
- 稳定 Release workflow：待回填。
- 稳定 Release：待回填。
- Release 状态：待回填。
- 发布资产：待回填。
- 正式 APK 大小：待回填。
- 正式 APK SHA-256：待回填。
- 签名证书 SHA-256：待回填。
- APK 签名校验：待回填。
- dev 对齐 CI：待回填。
- 用户通知：待稳定包可安装后按低频通知规则发送一次飞书通知，去重键 `release-v0.9.7`。
