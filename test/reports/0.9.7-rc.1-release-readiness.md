# TermuxPro 0.9.7-rc.1 候选发布评审记录

日期：2026-09-08

候选状态：`READY`。`dev` 自 `0.9.6` 稳定版后已积累通过门禁的用户可见增值改动，当前冻结为
`0.9.7-rc.1` 候选体验版。候选 Release 需等待发布分支 PR、分支 CI、Emulator UI、dev 收尾 CI 和
Release workflow 全部通过后更新为 `RELEASED`。

## 冻结范围

- PR #203：AI CLI 会话中心新增启动前远端环境预检入口。
- PR #204：AI CLI 会话中心展示工作区 tmux 策略和 AI 快捷启动策略边界。
- PR #205：AI CLI 会话中心按行动阶段分组，减少底部按钮堆叠和上下文判断成本。

## 增值服务准入

- 分类：AI CLI、远程工作区、tmux/Git 可视化、移动端工作流效率和上下文工具箱。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。
- 风险控制：AI 环境预检只读；AI 启动固定 `ssh_only`；tmux 必须由用户显式进入或管理；历史会话只进入
  Claude/Codex CLI 原生选择器。

## 候选门禁

- 本地版本、Skill、workflow、GitHub CLI、弹窗可读和 pre-push 冒烟必须通过。
- GitHub 分支 CI 必须通过。
- GitHub Emulator UI 必须通过。
- Release workflow 必须先完成待发布签名 APK 覆盖升级验收，再创建公开 Pre-release。
- 发布后必须核验 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt` 附件齐全。

## 当前验收记录

- 候选分支：`dev_release097Rc1_20260908`。
- 候选提交：以候选 PR head commit 为准，合并后补充 merge commit 和 Release run。
- 候选 PR：待创建。
- 本地门禁：通过 `termuxpro-version.sh` 版本/标签/Release Notes 检查、`validate-skills.sh`、
  `test/dialog-readable-style-test.sh`、`test/version-metadata-test.sh`、`test/workflow-trigger-policy-test.sh`、
  `test/release-apk-device-smoke-test.sh`、`git diff --check` 和
  `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/pre-push-smoke.sh origin/dev`。本轮为 release/docs/test
  证据改动，本地冒烟走静态门禁，不触发本机 Android 重构建。
- 分支 CI：待执行。
- 自动 PR 控制器：待执行。
- dev 收尾 CI：待执行。
- Release workflow：待执行。
- GitHub Release：待创建。
- Release 附件：待核验。
