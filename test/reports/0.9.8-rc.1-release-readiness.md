# TermuxPro 0.9.8-rc.1 候选发布评审记录

候选状态：`READY_FOR_RC_PR`。`0.9.7` 正式版后，`dev` 已合入两个用户可见增值切片，并完成本地验证、
GitHub CI、Emulator UI 和 dev 收尾 CI。当前冻结为 `0.9.8-rc.1`，不继续把已完成体验修复无限堆在 `dev`。

## 版本信息

- 候选版本：`0.9.8-rc.1`
- 候选 `versionCode`：`90801`
- 上个稳定版本：`0.9.7`
- 上个稳定 `versionCode`：`90702`
- 发布说明：`docs/RELEASE_NOTES_0.9.8-rc.1.md`

## 冻结范围

- PR #212：AI CLI 会话中心新增“运行项目任务”入口。
- PR #213：项目任务页显示执行目标上下文。

## 发布门禁

1. `termuxpro-version.properties` 必须为 `0.9.8-rc.1` / `90801`，且发布说明文件存在。
2. `versionCode=90801` 必须高于 `0.9.7` 正式版的 `90702`。
3. 本地 `git diff --check`、Skill 校验、版本元数据校验、workflow 策略校验、Release APK 设备冒烟脚本和
   Release ABI 门禁必须通过。
4. 候选 PR 合入 `dev` 前，分支 CI 和 Emulator UI 必须通过。
5. 候选 PR 合入 `dev` 后，merge commit 收尾 CI 必须通过。
6. 标签 `v0.9.8-rc.1` 必须由自动 PR 流水线在合并提交上创建，不覆盖既有标签。
7. Release workflow 必须创建公开 Pre-release，并完成正式签名 APK 构建、覆盖升级验收和附件校验。

## 本地验收

- `./scripts/termuxpro-version.sh name`：`0.9.8-rc.1`。
- `./scripts/termuxpro-version.sh code`：`90801`。
- `./scripts/termuxpro-version.sh notes`：`docs/RELEASE_NOTES_0.9.8-rc.1.md`。
- `./scripts/termuxpro-version.sh tag`：`v0.9.8-rc.1`。
- `./scripts/validate-skills.sh`：通过。
- `./test/version-metadata-test.sh`：通过，版本为 `0.9.8-rc.1 (90801)`。
- `./test/workflow-trigger-policy-test.sh`：通过。
- `./test/release-apk-device-smoke-test.sh`：通过。
- `./test/release-abi-gate-test.sh`：通过。
- `git diff --check`：通过。
- `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/pre-push-smoke.sh origin/dev`：通过；本候选准备分支只修改版本、
  发布说明和验收文档，静态门禁完成后不重复本地 Android 运行时编译。

## 远端验收

待 PR、Actions、Release 和 APK 附件核验完成后回填。
