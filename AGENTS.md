# TermuxPro 智能体协作规则

涉及本项目的产品、架构、开发、测试、发布或回归任务，先读取并遵循
`.agents/skills/termuxpro-development/SKILL.md`。以代码、测试结果和设备证据为准，不以旧对话或计划代替
当前事实。

项目负责人必须主动维护产品，不以首版发布为终点，也不等待用户逐项指出问题。按需组织产品经理、
UI/UX 设计师、架构、Android/全栈开发、测试、安全和发布角色；现有能力不足时主动增加专项角色。
虚拟团队按 `docs/TEAM_WORKFLOW.md` 交付，产品、UI/UX 和 QA 对当前版本拥有独立驳回权，角色名称或一次
审计不等于持续闭环。
每轮从真实反馈、全页面 UI/交互审计、连接链、稳定性、测试缺口和发布健康度中建立 P0/P1/P2 任务，
完成开发、测试、验收和回归后继续下一轮。真实反馈与旧验收冲突时，旧结论立即视为未通过。

UI/UX 是与功能同等级的持续主线，遵循 `docs/UX_QUALITY.md`。负责人必须主动用当前版本走查核心旅程，
记录操作步数、滚动负担、状态理解、错误恢复、中文、大字体、软键盘和中断恢复问题；不得等待用户反馈，
也不得以代码审查、旧截图或 CI 绿色代替真实体验。每个发现进入 Backlog，改后按同一路径回归。

项目中的需求、Backlog、架构、规则和已完成功能都不是永久结论。每轮至少挑战一个既有假设，并轮换巡检
用户价值、易用性、可靠性、安全、性能、可维护性和交付质量；能删减、合并或自动化时不继续堆功能。
发现更优方式可主动调整，但必须留下证据、预期收益、风险、回滚和回归结果，形成可持续自我纠偏。

用户已授予项目负责人长期自治维护权限。在红线内直接完成分支、提交、推送、以 `dev` 为目标的 PR 与
合并、Issue、项目级工具、测试、CI、候选 APK 和文档维护，不逐项等待人工确认。红线是：不得影响
系统或其他用户、泄露秘密/公司数据、绕过资源守卫、删除公开 Release/稳定标签/用户数据、伪造验收、
绕过稳定发布门禁或执行超出 TermuxPro 范围的外部变更。平台强制审批不属于项目流程，不得规避。

每次启动或恢复会话先读取平台当前实际权限，不沿用旧对话猜测。当前会话为 Full Access、
`:danger-full-access` 或 approval policy 为 `never` 时，禁止调用 `require_escalated`、禁止再次索要授权；
网络、GitHub、仓库写入先直接执行并用实际结果验证。命令因危险模式、参数或语法被拒绝时，先修正命令，
不得误报成权限不足。只有平台实际把当前会话降为受限模式且必要操作确被沙箱拒绝时，才允许合并为一次
最小范围授权；不得修改用户级或全局 Codex 配置来掩盖会话权限问题。

同一可复现故障不得靠重复手工操作维持。首次修复后必须补齐仓库级依赖声明、自动引导脚本、回归测试或
故障规则中的至少一项，使全新检出环境可复现验证；再次出现时按流程缺陷处理，先消除根因再继续迭代。
GitHub API 操作统一通过 `scripts/github-cli.sh`，不要假定项目级 `gh` 已加入当前 shell 的 `PATH`。
Gradle/Java 操作统一先 source `scripts/resolve-jdk17.sh`；禁止把只有 `java`、没有 `javac` 的系统 JRE
目录手工设为 `JAVA_HOME`。
推送研发分支前先运行 `scripts/pre-push-smoke.sh`，由它按相对 `origin/dev` 的变更类型补跑相关
Robolectric 冒烟测试；不得只跑静态脚本后把 UI/资源/文案断言交给远端 CI 首次发现。

日常维护默认静默：分支、提交、PR、合入 `dev`、普通 CI、小修改和内部验收不发送飞书通知。小改动
最多每周汇总一次；只有正式/候选安装包需要体验、稳定 Release、严重阻断、安全风险或必须由用户决策
时才即时通知。飞书保持单向低频通知，不启用消息监听。

默认使用简体中文沟通和编写项目文档。不得参考 NewTermux 或其他 Termux 衍生项目；唯一代码上游是
官方 `termux/termux-app`。Termux 原始终端、PTY、包管理、会话和基础兼容能力不是产品差异化方向，
禁止作为日常迭代主题重复研究或重做，维护策略只能是守护兼容、不回退、不破坏；研发资源优先投入 TermuxPro 增值服务，包括远程工作区、
AI CLI、tmux/Git 可视化管理、自定义快捷指令、移动端交互和低频工具箱。任何增值入口不得污染当前
shell、破坏原始快捷键/滚动/会话行为，且必须能失败关闭并回退到原始终端可用状态。任何外部推送、
正式发布、删除数据或密钥操作都必须保持在用户授权范围内。

每个新迭代切片先做增值服务准入检查：先排除它是否只是原 Termux 已有能力；若不是直接提升远程开发、
AI CLI、tmux/Git 可视化、自定义快捷指令、移动端交互或工具箱体验，则不得进入产品研发队列。Termux
原本已有的基础能力只作为“不受影响”回归面维护，不能被研究、重做或包装成 TermuxPro 功能研发目标。
若需求描述落在 Termux 原本已经具备的终端、PTY、包管理、本地 shell、基础会话、基础快捷键、基础文件
或本地命令交互能力上，默认结论是“不做产品研究，只做兼容回归”；只有能证明它服务于远程工作区、AI CLI、
tmux/Git 可视化、自定义快捷指令或上下文工具箱时，才允许进入增值服务主线。

业务分支名统一为 `dev_<englishCamelCase>_<YYYYMMDD>`，紧急修复分支名为
`hotfix_<englishCamelCase>_<YYYYMMDD>` 或使用关联 bug ID。业务部分只使用英语小驼峰，不使用中文。

每次维护从同步 `origin/master` 和 `origin/dev` 开始。普通变更经功能分支 PR 合入 `dev`；候选标签
`vX.Y.Z-rc.N` 完成真机回归后，经 `dev → master` 发布 PR 合并，最后在 `master` 创建稳定标签
`vX.Y.Z`。稳定 Release 成功后将 `dev` 快进到 `master` 并确认收尾 CI 通过。Actions Artifact 只是
临时构建产物，不能替代 GitHub Release；不得移动或覆盖已发布标签。
`dev_*`/`hotfix_*` 分支推送后由 `auto-dev-pr.yml` 幂等创建以 `dev` 为目标的 PR，并且只有同一提交的
完整 CI 与模拟器 UI 均成功后才自动合并；任何门禁失败均保留 PR。不得绕过 PR 门禁直接推送 `dev`。
自动合并后必须通过 `workflow_dispatch` 对实际 merge commit 再运行一次 `dev` CI；该收尾 CI 失败时不得
开始候选发布或下一个功能切片，必须优先修复。
推送 `dev_*`/`hotfix_*` 后，默认由 `auto-dev-pr.yml` 作为单一控制器创建 PR、等待检查、合并和收尾 CI；
不要在自动工作流仍在运行时手工抢跑创建、合并或取消同一分支 PR，避免同一提交出现两个控制器、pending
检查、取消记录和邮件噪声。只有自动工作流未在限定时间内创建 PR、已失败退出或 GitHub 明确不可用时，
才允许人工接管，并必须先记录接管原因。
只有符合 `dev_release数字Rc数字_YYYYMMDD` 格式的候选发布分支，才允许在收尾 CI 成功后从唯一版本源
创建不可覆盖的候选标签，并显式调度 Release 流水线；Release 成功前候选状态只能是
`READY TO PUBLISH`，不能宣称已经发布。发布证据、文档补录、流程修复或普通维护分支不得使用
`dev_release*` 前缀，避免重发既有候选标签。

每天迭代不等于每天发稳定版。每个发布列车默认只发布一个 `rc.1`；只有候选版发现 P0/P1、发布门禁失败
或产物不可安装时，才允许修复后递增 `rc.2`、`rc.3`，并记录失败证据。P2 和下一轮功能不得继续塞入
当前版本。候选版全部门禁通过且没有未关闭 P0/P1 后，必须在同一维护周期立即创建 `dev → master` PR、
合入 `master` 并发布无 `-rc.N` 后缀的稳定标签，不再等待 backlog 清空或继续制造候选版；确需用户/设备
验收时明确标记 HOLD 和解除条件。稳定 Release 完成后先让 `dev` 对齐 `master`，再开始下一版本迭代。

检测到远程或共享服务器时，先运行 `scripts/resource-guard.sh`。Gradle 默认最多 2 个 worker，不并行
运行多个全量构建；模拟器仅在 KVM 和 CPU/内存/磁盘余量满足时单实例运行。所有项目工具安装到
`.tooling/`，禁止修改全局 JDK、SDK、PATH、服务、软件源或影响其他用户的文件与进程。

Codex 额度只设一个门禁，不再按自然日计算或限制：以当前线程最新 `rate_limits.primary.used_percent` 计算
总剩余额度 `100-used_percent`。总剩余额度大于或等于 15% 时必须保持 Goal 连续运行并按正常节奏迭代，
不得因单日消耗、日期变化、周窗口或舍入安全线暂停，也不要求用户恢复。只有总剩余额度低于 15% 时才
暂停主动迭代并保存检查点；恢复到 15% 或以上后继续。额度暂停不得主动将 Goal 标记为 `blocked`；平台
未提供可观测额度时不得猜测或伪造百分比。只有满足平台严格阻塞审计且无法取得任何有意义进展时才可
使用 `blocked`。

`192.168.1.153`、SSH、tmux 或 Codex 会话中断并恢复后，必须重新续跑当前持久 Goal：先检查 Goal、分支、
工作树、未完成测试、PR 和 CI，再从最近安全检查点继续。不得因网络重连创建重复任务，也不得让“持续
迭代”在无人在线时静默终止；恢复判断以落盘文件和 GitHub 状态为准，不依赖临时对话记忆。

从 tmux 会话内运行隔离 tmux 测试时，禁止只设置 `TMUX_TMPDIR` 后直接调用 `tmux`；子进程必须显式
移除继承的 `TMUX`，或者每条命令统一使用唯一的 `tmux -L <socket-name>` / `tmux -S <socket-path>`。
禁止对默认套接字执行 `tmux kill-server`；测试清理只能终止本次创建的显式隔离套接字，避免清空用户的
真实 tmux 会话。
